{
  "devDependencies": {
    "axios": "1.7.5",
    "chai": "4.5.0", 
    "cross-env": "7.0.3",
    "mocha": "10.7.3",
    "mocha-jenkins-reporter": "0.4.8"
  },
  "scripts": {
    "test": "mocha",
    "test:fvt": "cross-env JUNIT_REPORT_PATH=target/report-fvt.xml mocha --timeout 999999 --recursive --colors --reporter mocha-jenkins-reporter"
  }
}
